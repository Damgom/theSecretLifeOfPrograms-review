# 비트

### 비트란

자연어에서 상자를 문자라고 부르듯, 컴퓨터 언어에서는 이를 `비트`라고 부른다.  
`비트` 라는 단어는 `바이너리(binary)`와 `디지트(digit)`가 합쳐진 단어이고, 비트를 사용하면 적은 비용으로 편리하게 기호를 담을 수 있다.
비트는 2진법을 사용하며, 2가지 기호 중 하나를 비트 상자에 담을 수 있다. (1과 0처럼)
